<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:StrataTheme.Controls">
  <Styles.Resources>
    <ResourceDictionary>
      <ControlTheme x:Key="{x:Type controls:StrataSetting}" TargetType="controls:StrataSetting">
        <Setter Property="FontFamily" Value="{DynamicResource Font.Family}" />
        <Setter Property="FontSize" Value="{DynamicResource Font.SizeBody}" />
        <Setter Property="Template">
          <ControlTemplate>
            <StackPanel>
              <!-- Top separator (visible between items in a group) -->
              <Border Name="PART_Separator"
                      Height="1"
                      Background="{DynamicResource Brush.BorderSubtle}"
                      Margin="0,8,0,8"
                      IsVisible="{TemplateBinding ShowSeparator}" />

              <!-- Setting row -->
              <Border Name="PART_RowBorder"
                      CornerRadius="{DynamicResource Radius.Base}"
                      Background="Transparent"
                      Padding="10,6">

                <Grid Name="PART_Row"
                      ColumnDefinitions="Auto,*,Auto"
                      RowDefinitions="Auto,Auto"
                      MinHeight="{DynamicResource Size.ControlHeightM}">

                  <!-- Highlight accent bar (left edge indicator for search) -->
                  <Border Name="PART_HighlightBar"
                          Grid.Column="0"
                          Width="3" CornerRadius="2"
                          Margin="0,2,8,2"
                          Opacity="0"
                          Background="{DynamicResource Brush.AccentDefault}">
                    <Border.Transitions>
                      <Transitions>
                        <DoubleTransition Property="Opacity" Duration="0:0:0.25" Easing="CubicEaseOut" />
                      </Transitions>
                    </Border.Transitions>
                  </Border>

                  <!-- Header + description + inline revert -->
                  <StackPanel Name="PART_Labels"
                              Grid.Column="1"
                              VerticalAlignment="Center"
                              Spacing="2"
                              Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <TextBlock Name="PART_HeaderText"
                                 Text="{TemplateBinding Header}"
                                 FontWeight="Medium"
                                 VerticalAlignment="Center"
                                 Foreground="{DynamicResource Brush.TextPrimary}">
                        <TextBlock.Transitions>
                          <Transitions>
                            <BrushTransition Property="Foreground" Duration="0:0:0.15" />
                          </Transitions>
                        </TextBlock.Transitions>
                      </TextBlock>
                      <!-- Revert indicator (inline with header) -->
                      <Button Name="PART_RevertButton"
                              Classes="subtle"
                              VerticalAlignment="Center"
                              Padding="4,2"
                              MinWidth="0"
                              MinHeight="0"
                              IsVisible="False"
                              ToolTip.Tip="Revert to default"
                              RenderTransformOrigin="0.5,0.5"
                              RenderTransform="scale(0.85)"
                              Opacity="0">
                        <Button.Transitions>
                          <Transitions>
                            <DoubleTransition Property="Opacity" Duration="0:0:0.18" />
                            <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.2" Easing="CubicEaseOut" />
                          </Transitions>
                        </Button.Transitions>
                        <TextBlock Text="↺" FontSize="12" />
                      </Button>
                    </StackPanel>
                    <TextBlock Name="PART_Description"
                               Text="{TemplateBinding Description}"
                               Classes="caption"
                               Foreground="{DynamicResource Brush.TextTertiary}"
                               TextWrapping="Wrap"
                               IsVisible="{TemplateBinding Description, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
                  </StackPanel>

                  <!-- Setting control -->
                  <ContentPresenter Name="PART_Content"
                                    Grid.Column="2"
                                    Content="{TemplateBinding SettingContent}"
                                    VerticalAlignment="Center" />
                </Grid>
              </Border>
            </StackPanel>
          </ControlTemplate>
        </Setter>

        <!-- Stacked: control drops below the labels and spans the full width. A side-by-side row
             assumes the control is narrow; on a touch layout the control is often a segmented
             picker or a full-width field, which otherwise crushes the description to a few words
             per line. -->
        <Style Selector="^.stacked /template/ ContentPresenter#PART_Content">
          <Setter Property="Grid.Column" Value="1" />
          <Setter Property="Grid.Row" Value="1" />
          <Setter Property="HorizontalAlignment" Value="Stretch" />
          <Setter Property="Margin" Value="0,10,0,0" />
        </Style>
        <Style Selector="^.stacked /template/ StackPanel#PART_Labels">
          <Setter Property="Margin" Value="0" />
        </Style>

        <!-- Modified: show revert button with scale-in -->
        <Style Selector="^:modified /template/ Button#PART_RevertButton">
          <Setter Property="IsVisible" Value="True" />
          <Setter Property="Opacity" Value="1" />
          <Setter Property="RenderTransform" Value="scale(1)" />
        </Style>

        <!-- Modified: accent the header text -->
        <Style Selector="^:modified /template/ TextBlock#PART_HeaderText">
          <Setter Property="Foreground" Value="{DynamicResource Brush.AccentDefault}" />
        </Style>

        <!-- Highlighted: search match accent bar + header color -->
        <Style Selector="^:highlighted /template/ Border#PART_HighlightBar">
          <Setter Property="Opacity" Value="1" />
        </Style>
        <Style Selector="^:highlighted /template/ TextBlock#PART_HeaderText">
          <Setter Property="Foreground" Value="{DynamicResource Brush.AccentDefault}" />
        </Style>

        <!-- Hover: subtle row highlight -->
        <Style Selector="^:pointerover /template/ Border#PART_RowBorder">
          <Setter Property="Background" Value="{DynamicResource Brush.SubtleHover}" />
        </Style>
      </ControlTheme>
    </ResourceDictionary>
  </Styles.Resources>
</Styles>
